//
// Created by Anna Bittencourt on 11/03/26.
//
#include <stdio.h>

int main() {
    // Type your code below
    int b1 = 5;
    int b2 = 4;
    int b3 = !((b1 + b2) > (b1 * b2)); // para retornar 1 a condição (b1 + b2) > (b1 * b2)) deve ser falsa, o ! inverte 

    // Don't change the line below
    printf("b3 = %d\n", b3);

    return 0;
}